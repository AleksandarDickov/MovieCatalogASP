@model IEnumerable<MovieCatalog.Models.Movie>

@{
    ViewBag.Title = "MOVIE LIST:";
}
 <h2>@ViewBag.Title</h2>




@foreach (var item in Model)
{
    <div>
        <h4>@item.Name</h4>
        <span>@item.Director</span>
        <p> @item.Genre , @item.ReleaseDate</p>
        
    </div>
}
